const CONSTANTS = {
  API_SERVER: 'http://localhost:8100',
  USER_LOCATION: new Date().getTime() * Math.random() * 100,
  USER_PASSWORD: new Date().getTime() * Math.random() * 100
};

export default CONSTANTS;
